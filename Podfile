# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DailyMed' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DailyMed

end

target 'myMed' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Alamofire', '~> 3.5.0'
    pod 'SwiftyJSON'

  # Pods for myMed

  target 'myMedTests' do
    inherit! :search_paths
    pod 'Alamofire', '~> 3.5.0'
    pod 'SwiftyJSON'
    # Pods for testing
  end

  target 'myMedUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
